### Set up conda env

Need to install pacbio packages and set up a conda env
```
conda create --name isoseq
```
install packages
```
conda activate isoseq
mamba install -c bioconda isoseq pbskera lima pbmm2 pbpigeon
```
